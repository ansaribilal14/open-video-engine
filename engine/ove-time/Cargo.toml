[package]
name = "ove-time"
version = "0.1.0"
edition = "2021"
description = "Exact rational time primitives for the open-video-engine timeline (ADR-007)"
license = "MIT OR Apache-2.0"

[dependencies]
